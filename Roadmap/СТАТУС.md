# Статус аудита — 23 августа 2026

Сводка того, что уже влито в `main` по пяти частям Roadmap.
Локально: **155 passed**. Actions после фикса `APP_ENV=test` (PR #4) — зелёные.

**Коммиты:** PR #1 `6776011` · PR #2 `d0eb934` · PR #3 `9da1e54` · CI-фикс в PR [#4](https://github.com/Ntxfloy/battlehall-loyalty-Ayderfix/pull/4)

---

## Гейт до запуска (сводный отчёт)

| # | Пункт | Статус |
| --- | --- | --- |
| 1 | HAR / JWT в git, `verify=False` | открыто — решение владельца |
| 2 | Убрать голый `require_admin`, проверить права | **сделано** — права на ручках, `pts.grant` |
| 3 | Не отдавать `webhook_token`; закрыть test-ручки | **сделано** |
| 4 | Сидер: отказ от `change-me` в проде | **частично** — слабый пароль владельца в prod блокируется |
| 5 | Условный UPDATE + `IntegrityError` на сессиях | **сделано** — PTS `RETURNING`, сессии ловят конфликт |
| 6 | `PRAGMA index_list` на боевом файле | открыто — ручная проверка на стенде |
| 7 | `total_earned` по белому списку | **сделано** — `EARNED_REASONS` + `WHEEL_PRIZE` |
| 8 | Возврат PTS при reject истёкшего кода | **сделано** |
| 9 | Sheets: не писать до коммита, `RAW` | **сделано** — claim/unclaim + `RAW` |
| 10 | Лимит `duration_minutes` | **сделано** — `MAX_SESSION_MINUTES=1440` |
| 11 | Телефон в реферальном `/start` | **сделано**; `notify()` по-прежнему нигде не вызывается |
| 12 | SQLite → Postgres | открыто |

---

## Часть 1 — Ядро

**Сделано**

- SQLite: WAL, `busy_timeout`, `foreign_keys=ON`
- rollback в `get_db`
- `settings_patch` в тестах (синглтон настроек больше не ломает прогон)
- `APP_ENV=test` в conftest + CI (PR #4)

**Открыто:** Alembic, деньги как `Decimal`, `CheckConstraint`, смена группы 1 января, `SecretStr`, зона как свойство клуба.

---

## Часть 2 — API / auth / вебхуки

**Сделано**

- права вместо голого `require_admin` на чувствительных ручках
- `webhook_token` больше не торчит в таблице клубов
- `/api/console/test/*` закрыт правом и выключен в prod
- `link-phone` не угоняет занятый номер (409)
- `/health` = `{"status":"ok"}`
- CORS / no-cache только в локальной среде
- демо-гейт: `safe_redirect` + `html.escape`
- PTS админки в JSON-теле, не в query
- безопасный разбор JSON на клиенте

**Открыто:** rate-limit логина, отзыв сессий, изоляция `club_id`, HMAC вебхука, CSP.

---

## Часть 3 — Сервисы

**Сделано**

- атомарный `pts._change_balance` через `UPDATE … RETURNING`
- `EARNED_REASONS` + отдельная причина `WHEEL_PRIZE`
- `expire_due` условным UPDATE, без двойного возврата
- reject истёкшего `submitted`-кода возвращает PTS
- реферал через `increment`, а не `mark_completed`
- нет `special_referral` — сессия всё равно закрывается
- неактивная/удалённая награда на ленте → возврат прокрутки
- `wheel.stats` считает `reward_payout_value`
- Sheets: `RAW` + захват строки (claim/unclaim)
- `IntegrityError` на повторном `start_session`

**Открыто:** взаимные рефералы, полный outbox Sheets, `spin_id` до flush, дедуп минут по `oasys_session_id`, `notify()`.

---

## Часть 4 — Фронтенд

**Сделано**

- tabbar на 5 колонок
- пункт меню **Коды**
- редактор прав вынесен из скрытого блока пользователей
- даты в мини-аппе/админке — Europe/Moscow
- `JSON.parse` в `try/catch`
- `esc()` на полях OASys

**Открыто:** оверлей прокрутки до `fetch`, `@media` админки, пагинация очереди, CSP, `Idempotency-Key`.

---

## Часть 5 — Бот / сессии / seed

**Сделано**

- реферальный `/start` просит телефон, если его нет
- `IntegrityError` на идемпотентности сессии
- потолок длительности сессии 24 часа
- слабый `ADMIN_DEFAULT_PASSWORD` в production не создаёт владельца
- `__test__ = False` на схемах `TestSession*Request`

**Открыто:** вызовы `notify()`, Alembic, мягкое удаление админов, второй владелец.

---

## Не трогали сознательно

Решения владельца: JWT/HAR в git, `oasys_live` + `verify=False`, переезд на Postgres, юридический статус внутреннего API OASys.
